apiVersion: kro.run/v1alpha1
kind: ClusterResourceQuotas
metadata:
  name: production
spec:
  teamName: production
  description: "Tracks all resource quotas for the Production team."
  budget:
    requests.cpu: "10"
    requests.memory: "20Gi"
    limits.cpu: "20"
    limits.memory: "40Gi"
